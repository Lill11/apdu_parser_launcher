# Sample Java Source Parser

This example is used for Phase B source installation.

Install it from the UI:

1. Open `More > Manage Parsers`
2. Click `Add Java Parser`
3. Select `examples\sample-source-parser\SourcePcscPlugin.java`

The source is compiled into an internal plugin JAR and then installed through the same plugin registry used for precompiled `.jar` plugins.
